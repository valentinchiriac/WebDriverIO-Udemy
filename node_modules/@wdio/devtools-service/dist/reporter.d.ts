import { default as WebdriverIOReporter, SuiteStats, TestStats } from '@wdio/reporter';
export declare class TestReporter extends WebdriverIOReporter {
    #private;
    constructor(options: any, report: (data: any) => void);
    onSuiteStart(suiteStats: SuiteStats): void;
    onTestStart(testStats: TestStats): void;
    onTestEnd(testStats: TestStats): void;
    onSuiteEnd(suiteStats: SuiteStats): void;
    get report(): Record<string, SuiteStats>;
}
