import { ServiceOptions, ExtendedCapabilities } from './types.js';
export declare class DevToolsAppLauncher {
    #private;
    constructor(options: ServiceOptions);
    onPrepare(_: never, caps: ExtendedCapabilities[]): Promise<void>;
    onComplete(): Promise<void>;
}
